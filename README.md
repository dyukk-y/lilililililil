# Telegram Bot для отслеживания активности

Telegram бот для отслеживания сообщений пользователей в определённой группе и вывода рейтинга активных участников.

## 🚀 Установка и запуск

### Предварительные требования
- Python 3.8+
- pip или conda

### Шаг 1: Клонирование репозитория
```bash
git clone https://github.com/dyukk-y/lilililililil.git
cd lilililililil
```

### Шаг 2: Создание виртуального окружения (рекомендуется)
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### Шаг 3: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 4: Конфигурация
1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Отредактируйте файл `.env` и добавьте ваши значения:
```bash
# Telegram Bot Token (получить у @BotFather в Telegram)
BOT_TOKEN=your_bot_token_here

# Channel and Chat IDs
MAIN_CHANNEL_ID=your_main_channel_id
COMMENTS_CHAT_ID=your_comments_chat_id
MODERATORS_CHAT_ID=your_moderators_chat_id
MODERATORS_TOPIC_ID=your_moderators_topic_id
ADMINS_CHAT_ID=your_admins_chat_id
ADMINS_TOPIC_ID=your_admins_topic_id

# Admin User IDs (comma-separated)
ADMINS=admin_id1,admin_id2

# Database
DB_NAME=smotrbot.db
```

**Как получить ID чата:**
- Напишите боту [@userinfobot](https://t.me/userinfobot) для получения ID
- Используйте команду `/start` в нужном чате и обратите внимание на ID

### Шаг 5: Запуск бота
```bash
python bot.py
```

## 📊 Функционал

- ✅ Отслеживание всех сообщений в определённой группе
- 🏆 TOP-20 пользователей по количеству сообщений
- 🔄 Кнопка обновления для актуализации рейтинга в реальном времени
- 🔒 Доступ к командам только для администраторов

## 📋 Команды

- `/top_comments` - показать TOP-20 пользователей по количеству сообщений

## 📦 Зависимости

- **aiogram** - фреймворк для работы с Telegram API
- **aiosqlite** - асинхронная работа с SQLite базой данных
- **python-dotenv** - загрузка переменных окружения из файла `.env`

## ⚙️ Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|---------|------------|
| `BOT_TOKEN` | Токен бота от BotFather | Да |
| `MAIN_CHANNEL_ID` | ID основного канала | Да |
| `COMMENTS_CHAT_ID` | ID чата для комментариев | Да |
| `ADMINS` | ID администраторов (через запятую) | Да |
| `MODERATORS_CHAT_ID` | ID чата модераторов | Нет |
| `ADMINS_CHAT_ID` | ID чата администраторов | Нет |
| `DB_NAME` | Имя файла базы данных | Нет |

## 🔒 Безопасность

- **Никогда** не коммитьте файл `.env` с реальными данными
- Используйте `.env.example` как шаблон
- Убедитесь, что `.env` добавлен в `.gitignore`

## 📝 Лицензия

MIT
